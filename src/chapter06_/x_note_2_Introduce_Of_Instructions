    字节码指令简介
        1.Java虚拟机的指令由一个字节长度，代表着某种操作含义的数字（称为操作码，OpCode）及跟随其后的零至多个代表此操作所需的参数（称为操作数，Operands）
    而构成。由于Java虚拟机采用面向操作数栈而不是寄存器的架构，所以大多数指令只有一个操作码，而没有操作数。
        由于操作码至多只有一字节长度，所以最多只支持256条指令
        1 >>> 字节码与数据类型
        2 >>> 加载和存储指令
        3 >>> 运算指令
        4 >>> 类型转换指令
        5 >>> 对象的创建与访问指令
        6 >>> 操作数栈管理指令
            将操作数栈的栈顶一个或两个元素出栈：pop/pop2
            复制操作栈顶一个或两个数值并将复制值或者双份的复制值重新压入栈顶：dup1,dup2,dup_x1,dup_x2,dup2_x1,dup2_x2
            将栈最顶端的两个值呼唤：swap
        7 >>> 控制转移指令
        8 >>> 方法调用和返回指令 invokedynamic，指令用于动态解析出调用点限定符所引用的方法，并执行该方法
        9 >>> 异常处理指令：athrow
        10 >>> 同步指令：monitorenter和monitorexit来支持synchronized关键字语义


