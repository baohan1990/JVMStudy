    Class类文件的结构

        Class文件的平台独立性是实现Java技术体系平台无关性的重要基石。

        编译生成的class文件（BytCode文件）是一个以八字节为基本单位的二进制流。各个数据项目严格按照顺序紧凑地排列在Class文件之中，中间没有添加任何分隔符，
    没有任何空隙存在。当遇到需要用到八位字节以上的数据项时，就会按照高位在前的方式分割成若干个八位字节进行存储。
        class文件中的两种数据类型，无符号数和表。
        无符号数属于基本数据类型，以u1,u2,u4,u8来分别表示1字节，2字节，4字节，8字节的无符号数，无符号数可以用来描述数字，索引引用，或者按照UTF-8编码构成
    字符串值。
        表是由无符号数或者其他表作为数据项构成的符合数据类型，所有表都习惯性的以_info结尾。
        无论是无符号数还是表，当需要表述同一数据类型但数量不定的多个数据时，经常会使用一个前置的容量计数器加若干个连续的数据项的形式，这时称这一连续的某
    一类型的数据为某一类型的集合。
        magic
        minor_version
        major_version
        constant_pool_count
        constant_pool
        ...
        1）访问标志（access_flags）